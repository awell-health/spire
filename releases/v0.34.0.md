## Features
- **Agentic recovery system** — Claude-driven decision making in the recovery formula: collect context, diagnose, choose action (resummon/reset/escalate), execute, verify, and write durable learnings (`spi-gre9p`)
  - `recovery_learnings` table for fast cross-bead learning queries (`spi-o7sry`)
  - Cross-bead learning query by failure class (`spi-nnu8y`)
  - Wired collect_context: diagnosis + per-bead + cross-bead learnings (`spi-36i9f`)
  - Claude decide step with ranked actions and prior learnings (`spi-is14l`)
  - Learn step: writes to bead metadata + recovery_learnings table (`spi-rxc9o`)
  - Updated recovery formula: agentic steps, always-close lifecycle (`spi-f8pga`)

## Fixes
- **Install deps before verify-build** — staging worktrees now run `runtime.install` (e.g. `pnpm install`) before build/test, fixing "command not found" failures in JS/TS repos (`spi-5hium`)
- **Recovery metadata in board inspector** — recovery beads now show source bead, failure class, resolution, verification status, and prior recovery history (`spi-5hium`)

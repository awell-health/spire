## Observability & Metrics (`spi-uprma`)
- **Full agent run recording** — all phases now tracked with timing buckets and phase attribution
- **Parent-child run linkage** — spawned agents linked to their wizard's run for end-to-end tracing
- **Per-phase token & cost breakdown** — understand where tokens and costs go across the pipeline
- **DuckDB OLAP layer** — daemon ETL with materialized views for fast analytics queries
- **Formula performance comparison** — compare metrics across formula versions
- **Week-over-week trend lines** — sparkline rendering on the metrics surface
- **K8s observability endpoints** — HTTP metrics surface on the steward pod

## Fixes
- **Escalation cascade prevention** — recovery beads no longer spawn infinite chains of recovery beads; circuit breaker stops at depth 1 (`spi-5hium`)
- **Wrong prefix on recovery/alert beads** — now inherit the parent bead's prefix (e.g. `ml-` not `spi-`) (`spi-5hium`)
- **Recovery formula rewrite** — steps now use `recovery.execute` dispatcher instead of spawning full wizard subprocesses with worktrees (`spi-5hium`)
- **Build command from spire.yaml** — embedded formulas no longer hardcode `go build ./...`; falls back to `runtime.build` from repo config (`spi-5hium`)
- **custom_types table sync** — fixes `design`/`recovery` type validation after beads v1.0.0 upgrade (`spi-5hium`)
- **Missing agent_runs columns** — `phase_bucket` and `timing_bucket` now created by `spire up` automatically (`spi-5hium`)
- **Daemon sync resilience** — timeout on dolt push/pull prevents hangs; failure state persisted immediately (`spi-bnzij`)

## Internal
- **beads v0.63.3 → v1.0.0** — major dependency upgrade with lipgloss v2 (`spi-9ky0e`)
- **`/release` skill** — Claude Code skill for structured release notes

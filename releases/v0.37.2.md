# v0.37.2

> Fix epic children leaking into board READY column.

## Fixes

- **Epic children no longer leak into READY column** — `ListBoardBeads` now populates dependencies before building BoardBeads, so `FindParentID` correctly identifies child beads (`spi-7uuy7`)
- **NULL issue conflicts resolved** — fix for dolt conflict resolution edge case

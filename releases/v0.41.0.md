## Features

- **`spire inject` for late-wave task dispatch** — new command writes tasks to graph state; the interpreter dispatches `InjectedTasks` before `NextSteps` at the next loop boundary (`spi-qfv1h`, `spi-5kjt6`)
- **Wizard message check at loop boundary** — wizard collects pending messages each graph iteration and stores them in `state.Vars` for downstream phases (`spi-1wobq`)
- **Atomic claim for cleric summon** — replaces the non-atomic registry check with an atomic claim pattern, eliminating races in multi-local setups (`spi-sg0fp`)

## Fixes

- **Base-branch override honored in sage review and merge** — sage review and merge now apply the bead's `base-branch:*` label override, so diff and merge target the correct branch (`spi-ybcpe`)

# v0.37.4

> Board attention line: compact interrupted + alert counts replace verbose banner.

## Improvements

- **Compact board attention line** — interrupted and alert counts shown as `⚠ N │ ⚑ N` on one line instead of listing each bead; press `v` to toggle to detailed views

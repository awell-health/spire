## Fixes
- **Recovery formula validation** — register `recovery.decide`, `recovery.learn`, `recovery.collect_context` opcodes so the agentic recovery formula passes validation and loads correctly
- **Concurrent release builds** — Linux and darwin goreleaser jobs now run in parallel

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spire-steward-data
  namespace: spire
  labels:
    app.kubernetes.io/name: spire-steward
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

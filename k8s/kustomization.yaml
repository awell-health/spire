apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Order matters: namespace → CRDs → storage → config → deployments
  - namespace.yaml
  - crds/wizardguild.yaml
  - crds/spireconfig.yaml
  - crds/spireworkload.yaml
  - beads-pvc.yaml
  - steward-pvc.yaml
  - beads-seed.yaml
  - spire-config.yaml
  - dolt.yaml
  - clickhouse.yaml
  - steward.yaml
  - operator.yaml
  - dolt-sync-rbac.yaml
  - dolt-sync-cronjob.yaml
  - network-policy.yaml
  - pod-priority.yaml
  - eso-external-secret.yaml
  # Optional — uncomment to deploy:
  # - syncer.yaml

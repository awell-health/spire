---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: spireconfigs.spire.awell.io
spec:
  group: spire.awell.io
  names:
    kind: SpireConfig
    listKind: SpireConfigList
    plural: spireconfigs
    shortNames:
    - sc
    singular: spireconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SpireConfig is the cluster-wide configuration singleton.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              defaultToken:
                type: string
              dolthub:
                properties:
                  credentialsSecret:
                    type: string
                  remote:
                    type: string
                required:
                - credentialsSecret
                - remote
                type: object
              enableLegacyScheduler:
                description: |-
                  EnableLegacyScheduler is the OperatorEnableLegacyScheduler gate
                  from spi-njzmg. When false (the canonical cluster-native default)
                  the operator does NOT start the legacy BeadWatcher or
                  WorkloadAssigner control loops; the operator only reconciles
                  pkg/steward/intent.WorkloadIntent into apprentice pods via
                  pkg/agent.BuildApprenticePod. When true, the legacy schedulers
                  start alongside the intent reconciler as a transitional
                  co-existence path. See pkg/config/deployment_mode.go for the
                  deployment-mode contract the gate composes with.
                type: boolean
              polling:
                properties:
                  interval:
                    type: string
                  reassignThreshold:
                    type: string
                  staleThreshold:
                    type: string
                type: object
              routing:
                items:
                  properties:
                    match:
                      additionalProperties:
                        type: string
                      type: object
                    token:
                      type: string
                  required:
                  - token
                  type: object
                type: array
              tokens:
                additionalProperties:
                  properties:
                    key:
                      type: string
                    secret:
                      type: string
                  required:
                  - key
                  - secret
                  type: object
                type: object
            required:
            - dolthub
            type: object
          status:
            properties:
              agentCount:
                type: integer
              beadCount:
                type: integer
              lastSync:
                type: string
              message:
                type: string
              workloadCount:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

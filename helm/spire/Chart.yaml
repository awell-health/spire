apiVersion: v2
name: spire
description: Spire — AI agent coordination hub for Kubernetes
type: application
# version and appVersion are auto-managed by goreleaser — do not edit by hand.
# They are held at the 0.0.0 sentinel in git; .goreleaser-linux.yaml rewrites
# both fields to the release tag (e.g. v0.42.0 → 0.42.0) via before.hooks
# before packaging chart artifacts. Image tags in values.yaml default to
# .Chart.AppVersion so `helm install` picks up the matching release images
# automatically.
version: 0.0.0
appVersion: "0.0.0"
keywords:
  - ai
  - agents
  - coordination
  - dolt
home: https://github.com/awell-health/spire
sources:
  - https://github.com/awell-health/spire
maintainers:
  - name: Awell Health
    url: https://awellhealth.com
